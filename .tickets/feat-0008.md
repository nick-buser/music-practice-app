---
branch: feat-0008
title: Idea page — body editor, attachments by revision, metadata editing (SB3b)
status: in-progress
---

## Description

Groomed as **SB3b** in `.tickets/_grooming-sketchbook-and-media.md`.

**Tier:** T2
**Depends on:** SB3a (merged, feat-0007). Consumes `downloadIdeaAsset` from
FX1 (merged, fix-0005).

**Why:** The stream is for capture; the idea page is where structure arrives
later.

**Scope / surfaces / files:**
- `app/src/views/IdeaPage.tsx` (rendered inside `SketchbookLive` on click):
  title (inline edit), body as markdown with the mock's `[section]` markers
  and `{ }` annotations preserved (reuse `LyricBlock` logic) plus `[[#n]]`
  rendered as links that navigate to that idea; save on blur/Ctrl-S via
  patch.
- Attachments panel: grouped by revision, each with filename, role chip,
  size, download link (`.../content`), inline `<audio>` for `audio/*`
  assets; upload button with role picker and a "new revision" checkbox.
- Metadata rail: status select, kinds + tags as editable chips, key /
  meter / bpm fields; links in/out list with kind labels.
- Props with lineage badges: placeholder section that PV3 fills
  (`<PropertiesPanel>` renders "no extracted properties yet").

## Acceptance criteria

- [ ] Body containing `[[#2]]` renders a link whose click selects idea with
      handle 2; editing and blurring calls patch with the new body —
      `IdeaPage.test.tsx` with a mocked client (substrate: unit)
- [ ] Attachments render grouped by revision; an `audio/webm` asset gets an
      `<audio>` element with the content URL (substrate: unit)
- [ ] Status change calls patch; adding a tag chip calls patch with the
      merged tag list (substrate: unit)
- [ ] Gates green (substrate: unit)
- [ ] After merge, on the dev slot: open idea #1, add tag `test`, reload, tag
      persists; upload a small `.opus`, the player appears and plays
      (substrate: deployed)

## Notes

**2026-09-02 — implemented and fully gated, NOT landed. Blocked on a
homelab outage, not on the work.**

The implementation is committed on `feat-0008` as `71ba2f2` and is
**unpushed** — it exists only on this laptop. `main` is in sync with
`gitea/main`; `71ba2f2` is the single commit at risk.

Gates, all re-run in the orchestrating session:

| Gate | Result |
|---|---|
| `npm run typecheck` | pass |
| `npm run gen:api:check` | pass |
| `npm run test` | pass — 26 files, **262** tests (+3 from `IdeaPage.test.tsx`) |
| `npm run build` | pass |
| `npm run test:e2e` | pass — **34 passed in 59.4s**, mock markup provably intact |

Acceptance criteria 1-4 met. Criterion 5 (`deployed`: open idea #1 on the
dev slot, add a tag, reload, upload an `.opus` and play it) is **not met
and could not be attempted** — it requires a merge and the dev slot, and
both are behind the outage.

What is down (checked 2026-09-02, ~21:30): `gitea`, `caddy`, `caddy-1`,
`geekom1`, `k3s-node`, `k3s-pve05`, `pve-05`, `pihole`, `pm`,
`research-lab`, `octoprint-pi` all offline on the tailnet within ~10
minutes of each other. Still up: `delegate`, `mlserve`, `pve`,
`pve-tower01`, `k3s-tower01`, `research-tower`. `.lab` names do not
resolve because `pihole` is the resolver, so there is no LAN fallback.

The branch was deliberately **not** pushed to `origin` (GitHub) — the
repo constraint forbids work branches on the public showcase mirror, and
an outage is not a reason to break it.

**To resume when the forge is back:** `git push -u gitea feat-0008`, open
the PR, merge, then close criterion 5 in a real browser against the dev
slot (the `local (e2e)` substrate now supports doing that properly).
