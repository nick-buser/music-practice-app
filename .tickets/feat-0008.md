---
branch: feat-0008
title: Idea page — body editor, attachments by revision, metadata editing (SB3b)
status: done
---

## Description

Groomed as **SB3b** in `.tickets/_grooming-sketchbook-and-media.md`.

**Tier:** T2
**Depends on:** SB3a (merged, feat-0007). Consumes `downloadIdeaAsset` from
FX1 (merged, fix-0005).

**Why:** The stream is for capture; the idea page is where structure arrives
later.

**Scope / surfaces / files:**
- `app/src/views/IdeaPage.tsx` (rendered inside `SketchbookLive` on click):
  title (inline edit), body as markdown with the mock's `[section]` markers
  and `{ }` annotations preserved (reuse `LyricBlock` logic) plus `[[#n]]`
  rendered as links that navigate to that idea; save on blur/Ctrl-S via
  patch.
- Attachments panel: grouped by revision, each with filename, role chip,
  size, download link (`.../content`), inline `<audio>` for `audio/*`
  assets; upload button with role picker and a "new revision" checkbox.
- Metadata rail: status select, kinds + tags as editable chips, key /
  meter / bpm fields; links in/out list with kind labels.
- Props with lineage badges: placeholder section that PV3 fills
  (`<PropertiesPanel>` renders "no extracted properties yet").

## Acceptance criteria

- [x] Body containing `[[#2]]` renders a link whose click selects idea with
      handle 2; editing and blurring calls patch with the new body —
      `IdeaPage.test.tsx` with a mocked client (substrate: unit)
- [x] Attachments render grouped by revision; an `audio/webm` asset gets an
      `<audio>` element with the content URL (substrate: unit)
- [x] Status change calls patch; adding a tag chip calls patch with the
      merged tag list (substrate: unit)
- [x] Gates green (substrate: unit)
- [x] After merge, on the dev slot: open idea #1, add tag `test`, reload, tag
      persists; upload a small `.opus`, the player appears and plays
      (substrate: deployed). **MET 2026-09-02**, 9/9 checks in a real
      browser — see Notes.

## Notes

Merged as PR #18 (`c145b47`). Full account in
`work_history/feat-0008.md`.

**Deployed criterion — MET, 9/9.** Driven in the installed Google Chrome
via `channel: 'chrome'` (FX2), against `soundings-dev`, with a 1-second
440 Hz sine encoded to Opus locally by `ffmpeg` (zero downloads):

- live sketchbook rendered (not the mock), 4 idea cards
- idea #1 opens the idea page
- tag `test` added and **survives a full reload** — persisted server-side
- an `<audio>` player is present for the opus, `src` = the content route
- that route returns **`audio/ogg`, 3989 bytes, byte-identical to the local
  file** — the Garage round trip is real
- the player **plays**: `readyState 4`, `duration 0.9935s`, `currentTime`
  advanced to `0.9595`
- no uncaught page errors

**Ticket history:** implemented and fully gated during a homelab outage
(`gitea`, `caddy`, `pihole`, the k3s dev slot and six more hosts offline);
the commit sat unpushed on the laptop until the forge returned, and was
deliberately never pushed to the GitHub mirror. The loop terminated during
the outage rather than polling, per progress-or-death.

**Found in flight:** the `(untitled capture)` headline overflows its
container on the idea page — filed as **FX3** in the grooming doc.
