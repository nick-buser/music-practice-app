---
branch: docs-0005
title: sketchbook, media, and roadmap grooming
status: done
tier: T2
---

## Description

Grooms the implementation seeds of `docs/sketchbook.md`,
`docs/recordings-provenance.md`, `docs/score-substrate.md`, and
`docs/sight-reading-generation.md` into `.tickets/_grooming-sketchbook-and-media.md`
now that the k3s deploy gate has cleared. Twenty-one loop-runnable tickets
across media plumbing, sketchbook, provenance/jobs, recordings, and CI; two
F-class spec reviews (ScoreDoc + anchors; technique taxonomy) called out first
and separately, gating the score-substrate and sight-reading spines, which
are listed provisionally (`[not seeded]`) until the reviews land.

Side effects: `.tickets/loop.md` brought up to the ticket-loop template
(`## Runner`, `## Substrates`, `## Merge policy`, `## Model routing`, new
queue order); `_grooming-k3s-onboarding.md` flipped to `status: done` (every
ticket in it is claimed and merged).

## Acceptance criteria

- [x] Every proposed ticket carries a tier, a grounded Why, concrete
      scope/files, and acceptance criteria that name a command and a
      substrate (unit / ci / deployed).
- [x] No branch numbers in headings; stable labels only.
- [x] F-class shortlist leads the doc and is not folded into T3.
- [x] Audit method stated; claims verified against code, chart values,
      Woodpecker workflows, the infra mlserve playbook, and the REAPER
      repo's ticket state.
- [x] Docs-only diff: clean `git status` after commit; PR merged.

## Notes

- Playwright browsers are absent on this laptop, so `local (e2e)` is a
  down substrate; recorded in loop.md rather than assumed.
- The REAPER sidecar schema does not exist (service-0005 unclaimed), so the
  inbox receiver (SB8) is not seeded.
