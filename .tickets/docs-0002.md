---
branch: docs-0002
title: loop-config
status: done
---

## Description

Prepare the ticket-loop harness for the k3s-onboarding queue: add
`.tickets/loop.md` (gates, verify, constraints, queue) and amend
`_grooming-k3s-onboarding.md` so every unit is loop-pickable or explicitly
H-gated — split old Ticket 1 into feat-0001 (code, tier S) and ops-0001
(Woodpecker wiring, tier H), tag tiers for delegation routing, record the
already-done forge onboarding, and update the ordering/eligibility summary.

## Acceptance criteria

- [x] `.tickets/loop.md` exists with runnable gates for both packages and
      the repo's hard constraints (Gitea forge, showcase isolation, no
      local container runtime, cross-repo claim rules, H-ticket handling).
- [x] Grooming doc has no ticket that mixes agent work with
      classifier-blocked operator steps.
- [x] Every ticket carries a tier tag; ordering section states loop
      eligibility.

## Notes

Friction (dogfooding): branch-new wrote this stub before the grooming
amendments existed — stub-then-backfill is fine but means the ticket file
and grooming doc are edited in the same PR; acceptable for docs tickets.
