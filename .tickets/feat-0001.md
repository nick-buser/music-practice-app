---
branch: feat-0001
title: containers-docker-ci
status: in-progress
---

## Description

Groomed Ticket 1a (tier S) from `_grooming-k3s-onboarding.md`: container
images + docker CI, code only. Web image (multi-stage node → nginx with SPA
fallback, `VITE_API_BASE_URL=/api` build arg), opt-in migrations in the api
entrypoint (`RUN_MIGRATIONS`, default on for compose parity), and
`.woodpecker/docker.yml` buildx pipeline pushing `soundings-api` +
`soundings-web` sha8 images to the Gitea registry. CORS env override
confirmed.

## Acceptance criteria

- [ ] Repo gates green (`.tickets/loop.md`); frontend build succeeds with
      `VITE_API_BASE_URL=/api` (backend features compiled in).
- [ ] Image builds NOT verifiable on this laptop (no container runtime) —
      PR body says "image build verified in CI only".
- [ ] Existing compose flow unbroken (`RUN_MIGRATIONS` defaults on).

## Notes

Implementation delegated to a sonnet-class subagent (tier S); gates run in
the orchestrating session.
