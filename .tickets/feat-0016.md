---
branch: feat-0016
title: export bundle — manifest schema, round-trip test, directory and zip sinks (SB6)
status: done
---

## Description

Groomed as **SB6** in `.tickets/_grooming-sketchbook-and-media.md`.

**Tier:** T1
**Depends on:** SB2 (merged, feat-0006)

**Why:** The ten-year guarantee. Manifest first, then sinks.

**Scope / surfaces / files:**
- `backend/app/export/manifest.py`: pydantic `IdeaManifest`
  (`schema_version: 1`, id, handle, title, status, kinds, tags, key, meter,
  bpm, links by handle, assets with role/filename/sha256/revision/producer,
  properties with lineage — properties empty until PV1 lands, shape fixed
  now).
- `backend/app/export/bundle.py`: `export_idea(db, store, idea) ->
  Iterator[(path, bytes)]` yielding `manifest.yaml`, `notes.md`,
  `assets/<revision>/<filename>`; `import_bundle(db, store, entries)` that
  recreates rows (new ids, same handles if free) and re-puts bytes.
- Sinks over the same iterator: `write_directory(path)`,
  `GET /v1/ideas/{id}/export` streaming a zip.

## Acceptance criteria

- [x] Round trip: create an idea with two assets and one link, export to a
      temp dir, import into a fresh DB + store, compare rows field-by-field
      (excluding ids/timestamps) and asset sha256s — `tests/test_export.py`
      (substrate: unit)
- [x] The zip endpoint returns a valid archive whose `manifest.yaml`
      validates against `IdeaManifest` (substrate: unit)
- [x] Gates green; contract regenerated (substrate: unit)

## Notes

All three criteria are `unit`, so this ticket closes locally — no `ci` or
`deployed` dependency.

`pyyaml` 6.0.3 is already installed as a runtime transitive of
`uvicorn[standard]` (confirmed with `uv tree`), so it is present in the
production image and declaring it explicitly in `[project.dependencies]`
downloads nothing — this respects the disk-discipline rule while not
leaving production code depending on an undeclared transitive.
