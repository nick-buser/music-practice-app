---
branch: docs-0004
title: musical sketchbook design + vendored source
status: done
---

## Description

Vendors the 2026-08-29 "sketchbook owns the ideas; REAPER is one editor"
conversation verbatim (`docs/sketchbook-vendored.md`) and writes the house
design that adopts it as the sixth roadmap workstream
(`docs/sketchbook.md`): under-typed `ideas` with `[[#n]]` links, assets
with `revision` + `run_id` (raw immortal / derived recomputable applied to
bytes), typed edges for the creative DAG (linear inside an idea, DAG
between), collections over subjects, inbox/quick-capture, provenance-named
MIDI features + FluidSynth previews on mlserve, portability as an export
bundle (no Git store), and the three REAPER seams (inbound sidecar from the
REAPER repo's service-0005, outbound bundle → scratch project, render
farm on the studio host) hooked to the homelab music-VM state.

Side effects: recordings-provenance.md gains the subject-keyed runs
amendment; the k3s grooming doc records the verified 2026-08-29 substrate
state (infra key-turn done via fix-0219, Woodpecker secrets still unset).

## Acceptance criteria

- [x] Vendored text is verbatim, with a provenance preface only.
- [x] Design doc follows house conventions (prose-first rationale,
      decisions-worth-knowing, deliberately-not-yet, tiered seeds) and
      cross-links score-substrate, recordings-provenance, and the REAPER
      repo docs it depends on.
- [x] Cross-repo claims verified against live state (Woodpecker API,
      homelab + REAPER repo surveys) and recorded with ticket ids.
- [x] Docs-only diff: clean `git status` after commit; PR merged.

## Notes

Next after merge: groom `docs/sketchbook.md` seeds (plus the un-groomed
seeds of the three docs-0003 docs) into `.tickets/_grooming-*.md`; the
deploy gate for all of it is still ops-0001 → T3 → T4.
