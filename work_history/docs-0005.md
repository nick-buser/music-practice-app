# docs-0005 — sketchbook, media, and roadmap grooming

- **Merged:** PR #8, 2026-09-02.
- **What:** `.tickets/_grooming-sketchbook-and-media.md` — the seeds of
  `docs/sketchbook.md`, `docs/recordings-provenance.md`,
  `docs/score-substrate.md`, and `docs/sight-reading-generation.md` groomed
  into 21 loop-runnable tickets (MD1; SB1–SB7 + SB8 not seeded; PV1–PV5;
  RC1–RC6; OPS1 decision-gated, OPS2) with a command and a substrate per
  acceptance criterion, plus two F-class spec reviews (F1 ScoreDoc + anchor
  contract, F2 technique taxonomy) that gate the provisional SC and SR
  tables. `loop.md` brought up to the ticket-loop template (Runner,
  Substrates, Merge policy, Model routing, queue order).
  `_grooming-k3s-onboarding.md` flipped to done.
- **Gates:** docs-only — clean tree after commit.
- **Grounding:** backend/app + migrations + tests read in full; frontend
  grepped for backend callers and for Web MIDI / MediaRecorder (none);
  gitops `charts/soundings/values*.yaml` for the S3 env names; infra
  `mlserve-setup.yml` for the service-add pattern (ports 8001–8004 taken);
  REAPER `_grooming-platform-and-capture-pilot.md` Ticket 15 (service-0005
  unclaimed → no sidecar schema); Gitea open PRs (none); Playwright browser
  cache (absent → e2e substrate down on this laptop).
- **Decisions parked for Nick:** dispatch F1/F2 (or waive and seed SC1/SR1
  at T3); OPS1 preview slot (dev slot tracks PR builds — yes/no).
- **Friction:** `tea pulls list` needs `--repo` from a subdirectory; BSD sed
  has no `0,/re/` range; loop.md's S/O/H tier tags predated the T-ladder and
  needed a mapping line rather than a rewrite of the closed doc.
