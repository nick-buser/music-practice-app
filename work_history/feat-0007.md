# feat-0007 — SB3a: Sketchbook live stream, inbox filter, quick capture

- **PR:** #15 on Gitea, opened and merged 2026-09-02 (`fcde2e7`). Sixth
  ticket of the post-F1/F2 ticket-loop run, and the first frontend one.
- **What:** `SketchbookView.tsx` becomes a thin switch on `backendEnabled`;
  the whole existing mock moves verbatim to `SketchbookMock.tsx`.
  `SketchbookLive.tsx` renders a reverse-chronological stream with an inbox
  filter and a quick-capture box (textarea, optional file, `c` hotkey) that
  POSTs an inbox idea and uploads any chosen file with a mime-guessed role.
  `api/ideas.ts` over the generated client, `useIdeas.ts` mirroring
  `useSavedChords`, DTO aliases from `components['schemas']`.
- **Gates:** typecheck, `gen:api:check` (in sync, **no** regeneration),
  vitest **259 passed** across 25 files, build. Re-run in the orchestrating
  session. Backend gates not run — no `backend/**` file touched. CI:
  pipelines #28 (branch) and #29 (PR) green.
- **Routing:** T2 → sonnet. One dispatch, no retry.

## The claim no gate here could check, checked anyway

`app/e2e/stats-and-sketchbook.spec.ts` pins `.sketch-grid`,
`.lyric-block .marker`, `.sketch-detail .tabs button`, `.chord-row .chord`
(exactly 5) and `.harmony-score svg g.note`. Playwright browsers are absent
by disk policy, so **no gate available on this laptop can catch a break in
that markup** — which makes "I moved it verbatim" a claim worth verifying.

```
diff <(git show HEAD:app/src/views/SketchbookView.tsx) app/src/views/SketchbookMock.tsx
29c29
< export function SketchbookView() {
---
> export function SketchbookMock() {
```
One line, the export rename. `git diff app/src/styles/app.css` is empty:
zero new CSS, existing classes reused.

## Sidebar count

`NAV` in `Sidebar.tsx` is a module const and cannot read live state, so
`Sidebar` gained an optional `counts` override falling back to each item's
static value, leaving the `.count`/`.active` markup untouched. `App.tsx`
computes the live number via its own `useIdeas(true)`, independent of the
instance `SketchbookLive` owns. Accepted trade-off: two `GET /v1/ideas`
while the tab is open, and a badge that trails a capture until its next
fetch. Lifting the hook into shared context is the fix if that ever reads as
a bug; it is a bigger change than SB3a should carry.

## `deployed` criterion — PARTIALLY verified. Read this before trusting it.

The criterion is: *on the dev slot, capture "test #1" from the UI, reload, it
is in the stream; the sidebar count reads the inbox size.*

**Verified:**
- `soundings-web` rolled to `fcde2e74`, 1/1 replicas available.
- The **deployed bundle genuinely contains SB3a's live view** — its unique
  strings are present in `/assets/index-lCPzApB5.js`:
  `"Catch the thought before it sounds away…"` (the capture placeholder) ×2
  and `"Attach a file"` ×1. So the shipped build is SB3a's, not the mock.
- `"/api"` is baked into the bundle (the web image is built with
  `VITE_API_BASE_URL=/api`, see `.woodpecker/docker.yml`), so
  `backendEnabled` is true in the deployed app and `SketchbookLive` is the
  branch that renders.
- The data path the capture box uses works end to end against the dev slot:
  `POST /api/v1/ideas {"body":"test #1"}` → **201, handle 3, status inbox**;
  a subsequent `GET /api/v1/ideas` (what a reload issues) returns
  **total 3, inbox count 3, newest-first true**, with `test #1` at the head
  of the stream above SB2's and SB1's rows.

**NOT verified: the DOM interaction itself.** The Claude-in-Chrome extension
is not connected in this session ("Browser extension is not connected"), and
Playwright is deliberately absent, so nothing here could drive a real
browser. What remains unproven is that the React components actually render
that data, that the `c` hotkey fires, and that the inbox toggle filters — the
component behaviour is covered by `SketchbookLive.test.tsx` against a mocked
client, but not against the live deployment.

The honest summary: **the build is right and the data path is right; the
rendering was not observed in a browser.** Finishing it needs one browser
pass over `https://soundings-dev.k8s.bittern-chameleon.dev/` with the
extension connected.

## Contract bug found in flight, filed not fixed

The SB2 streaming download route is documented in `openapi.json` as
`{"content": {"application/json": {"schema": {}}}}` although it returns real
binary — the SB2 deploy check round-tripped 1 MB with a correct
`Content-Type` and `ETag`. Every generated client therefore types the
response as JSON `unknown`, which is why no typed `downloadIdeaAsset` exists.
Filed as **FX1** in the grooming doc (T0). Note what this says about gates:
SB2 was green everywhere and its runtime check passed, because the defect is
in the *documentation of a route that works*.

## Friction (dogfooding)

- The Claude-in-Chrome extension being disconnected turns any
  "drive the deployed UI" criterion into a partial. SB3b, SB4 and RC2 all
  carry criteria of that shape; either the extension gets connected for
  those, or their deployed criteria should be re-groomed down the substrate
  ladder to something checkable headlessly.
