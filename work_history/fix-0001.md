# fix-0001 — heatmap-utc-dates

- **Merged:** PR #3, 2026-07-11.
- **What:** `buildHeatmap()` grid arithmetic moved to UTC methods; fixes the
  timezone-dependent "starts on a Sunday" test failure (pre-existing on
  main, surfaced by feat-0001's gate run — CI had never run before today).
- **Gates:** frontend suite green, 252/252.
- **Friction:** none; the fix-first-then-feature sequencing kept the gate
  honest without blocking the queue.
