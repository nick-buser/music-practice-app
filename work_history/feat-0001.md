# feat-0001 — containers-docker-ci

- **Merged:** PR #4, 2026-07-11.
- **What:** app/Dockerfile.web + nginx.conf (SPA fallback, /healthz, asset
  caching) + app/.dockerignore; RUN_MIGRATIONS opt-in (default 1) in
  backend/docker-entrypoint.sh; .woodpecker/docker.yml buildx pipeline
  (soundings-api, soundings-web; sha8+latest; main-only). CORS: already
  env-overridable, no change.
- **Gates:** frontend 252/252 + build (with VITE_API_BASE_URL=/api),
  backend ruff/pyright/pytest all green. Image builds CI-only (no local
  runtime by design); pipeline pushes will fail at registry auth until the
  ops-0001 operator key-turn (curls in grooming doc Notes).
- **Delegation:** implemented by a sonnet-class subagent (tier S); gates run
  in the orchestrating session. Gate run surfaced pre-existing red →
  fix-0001 (landed first).
