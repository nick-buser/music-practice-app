# docs-0006 — F1 + F2 frontier spec reviews

- **PR:** #9 on Gitea, opened 2026-09-02 — **open, awaiting Nick's
  ratification** (substrate H; the merge is the ratification). No
  conflicts with `main`.
- **What:** the two F-class reviews from
  `.tickets/_grooming-sketchbook-and-media.md`, dispatched together.
  `docs/score-substrate.md` (F1) and `docs/sight-reading-generation.md`
  (F2) amended in place, each ending with a "what changed and why" list;
  cross-doc amendments to `docs/recordings-provenance.md` and
  `docs/sketchbook.md`; SC1 and SR1 seeded as full tickets and the SC/SR
  spines re-tabled with corrected dependencies; `.tickets/loop.md` routing,
  substrates and an Alembic rule; `docs/probes/verovio/` (25 node scripts +
  `results.txt`) as re-runnable evidence for every "verified 4.5.1" claim.
- **Gates:** docs-only diff — clean tree after each of the ten commits;
  `sh docs/probes/verovio/run-all.sh` green against `app/node_modules/verovio`
  4.5.1.
- **Method:** thirteen adversarial lens finders (332 findings; the
  two-refuter verification stage hit the session limit after 3, so the
  synthesis was done by hand with every empirical claim re-run locally),
  then a six-critic pass over the amended text (144 findings; the real ones
  applied, eleven of them re-probed in `exp22`).
- **Highest-impact finds:** Verovio applies no key signature to sounding
  pitch (`exp12`) and beams no MEI input (`exp09`) — either would have
  shipped through every gate; the timemap lists tie-stops as onsets
  (`exp11`); ULIDs are neither deterministic nor NCNames; the taxonomy's
  rhythm/syncopation/meter/hands ladders were mis-ordered against every
  graded syllabus; the original calibrator rule let only focus dimensions
  rise and counted its own probes as failures.
- **Decisions parked for Nick:** the provisional L1–L10 preset table
  (ABRSM-anchored from memory); system marks as virtual projections, never
  rows; `attempts` without soft delete (void); the matcher as a
  client-executed run (PV1 grows a completed-run body).
- **Friction:** the first workflow's 560 verifier agents tripped the
  session limit — 13 finders × ~25 findings × 2 refuters is too many for
  one run; next time verify only `high` findings, or triage by hand first.
  `branch-new` fetches `origin` (GitHub) rather than `gitea`; harmless here
  because both mains were equal. The Bash tool's cwd drifted into the
  scratchpad once and a `cp` landed in the wrong tree — use absolute paths.
  A `rm -rf` inside a compound command was denied by the classifier; avoid
  it. Verovio probes must import `app/node_modules/verovio/dist/*.mjs` by
  path (the package has an exports map but no bare-specifier resolution
  from outside `app/`).
